# fakenewsdetector
A machine learning-based web app that classifies news articles as real or fake using Natural Language Processing. The model is trained on labeled datasets with text preprocessing (cleaning, stopword removal) and TF-IDF vectorization, followed by a classification algorithm like PassiveAggressiveClassifier.
